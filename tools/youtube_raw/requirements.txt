# Estágio 1 (captura) — sem estes, a nota sai sem a seção de transcrição.
youtube-transcript-api>=0.6

# Estágio 2 (enriquecimento) — só necessário para `python3 -m tools.youtube_raw.enrich`.
anthropic>=1.0
